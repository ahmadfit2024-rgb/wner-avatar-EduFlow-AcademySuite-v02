from django.urls import path
# from .views import MyCertificatesView

app_name = 'enrollment'

urlpatterns = [
    # Add frontend enrollment URLs here later
    # path('my-certificates/', MyCertificatesView.as_view(), name='my_certificates'),
]